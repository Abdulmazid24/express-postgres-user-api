# Back to Basics: Relational Database & Express Core Mechanics

**Author:** Abdul Mazid  
**Profile:** Full-Stack MERN Developer | React, Node.js, MongoDB  
**Date:** May 15, 2026

একজন ফুল-স্ট্যাক MERN ডেভেলপার হিসেবে MongoDB এবং Mongoose এর ম্যাজিক ব্যবহার করে স্কেলেবল (Scalable) প্রজেক্ট বানানো আমার প্রতিদিনের কাজ। কিন্তু ফ্রেমওয়ার্ক বা ORM এর ম্যাজিকের ওপর নির্ভরশীল হয়ে থাকলে একজন সাধারণ "কোডার" থেকে "ওয়ার্ল্ড-ক্লাস ইঞ্জিনিয়ার" হওয়া যায় না। 

তাই আমার ইঞ্জিনিয়ারিং ফাউন্ডেশনকে আরও "Bulletproof" করার জন্য আমি সিদ্ধান্ত নিয়েছি রিলেশনাল ডাটাবেস (PostgreSQL) এবং Express.js এর কোর মেকানিজমগুলো একদম রুট লেভেল (Root level) থেকে আবার স্টাডি করার। 

এই প্রজেক্টে আমি ইচ্ছে করেই Prisma বা TypeORM এর মতো কোনো ORM ব্যবহার করিনি। আমি র-এসকিউএল (Raw SQL) এবং `pg` ড্রাইভার ব্যবহার করে ডাটাবেসের সাথে সরাসরি যোগাযোগ করেছি। এটি কোনো সাধারণ লার্নিং নোট নয়, এটি ডাটাবেস ইঞ্জিন এবং ব্যাকএন্ড আর্কিটেকচার নিয়ে আমার ডীপ-ডাইভ (Deep-Dive) লার্নিং ডায়েরি।

---

## ১. ডাটাবেস আর্কিটেকচার ও পুলিং (The Engineering Behind Connection)

### ১.১. TCP/IP হ্যান্ডশেক এবং Connection Pooling
Mongoose ব্যবহার করার সময় আমরা `mongoose.connect()` লিখে ভুলে যাই ব্যাকএন্ডে কী হচ্ছে। কিন্তু PostgreSQL এ `pg` ড্রাইভার নিয়ে কাজ করার সময় আমি `Client` এর বদলে `Pool` ব্যবহার করেছি।
```typescript
const pool = new Pool({ connectionString: config.connection_string });
```
**The Engineering Logic:** ডাটাবেসের সাথে একটি নতুন কানেকশন তৈরি করা (TCP Handshake, Authentication, SSL Negotiation) অত্যন্ত সময়সাপেক্ষ কাজ। প্রতিটা API রিকোয়েস্টের জন্য নতুন কানেকশন বানালে সার্ভারের পারফরম্যান্স ড্রপ করবে। `Pool` এই কাজটা অপ্টিমাইজ করে। এটি আগে থেকেই ১০-২০টি কানেকশন তৈরি করে RAM এ ধরে রাখে (Warm connections)। যখনই কোনো ক্লায়েন্ট রিকোয়েস্ট আসে, সে সাথে সাথে একটি কানেকশন প্রোভাইড করে, ফলে লেটেন্সি (Latency) অভাবনীয়ভাবে কমে যায়।

### ১.২. Neon Serverless Architecture
আমি ডাটাবেস হিসেবে Neon DB ব্যবহার করেছি। এটি আধুনিক "Scale-to-Zero" সার্ভারলেস আর্কিটেকচার ফলো করে। অর্থাৎ, যখন আমার অ্যাপ্লিকেশনে ট্রাফিক থাকে না, ডাটাবেসের কম্পিউট ইঞ্জিন অটোমেটিক ঘুমিয়ে পড়ে (Sleep mode) এবং খরচ বাঁচায়। রিয়েল-ওয়ার্ল্ড ডিস্ট্রিবিউটেড সিস্টেমে (Distributed Systems) এই কনসেপ্টটি জানা অত্যন্ত জরুরি।

---

## ২. র-এসকিউএল এবং মেমোরি ম্যানেজমেন্ট (Raw SQL Mechanics)

### ২.১. Schema Creation & Indexing
```sql
CREATE TABLE IF NOT EXISTS users(
  id SERIAL PRIMARY KEY,
  name VARCHAR(20),
  email VARCHAR(20) UNIQUE NOT NULL
)
```
*   **`SERIAL` বনাম UUID:** `SERIAL` ডাটাবেসের ভেতরে একটি সিকোয়েন্স তৈরি করে এবং Auto-incrementing Integer জেনারেট করে। এটি মেমোরিতে মাত্র 4 Bytes জায়গা নেয়, যেখানে UUID নেয় 16 Bytes। 
*   **`PRIMARY KEY` এর ম্যাজিক:** প্রাইমারি কী ডিক্লেয়ার করার সাথে সাথে PostgreSQL ব্যাকএন্ডে একটি B-Tree ইনডেক্স (Index) তৈরি করে ফেলে। এর ফলে ভবিষ্যতে ১ মিলিয়ন ইউজারের মধ্যে আইডি দিয়ে কাউকে খুঁজলে `O(log n)` টাইম কমপ্লেক্সিটিতে ডাটা পাওয়া যায়।

### ২.২. SQL Injection Prevention
```typescript
await pool.query(`INSERT INTO users(name,email,password,age) VALUES($1,$2,$3,$4)`, [name, email, password, age]);
```
Mongoose অটোমেটিকভাবে ডাটা স্যানিটাইজ (Sanitize) করে। কিন্তু Raw SQL এ হ্যাকাররা ইনপুট ফিল্ডে SQL কমান্ড (`'; DROP TABLE users; --`) লিখে দিতে পারে। একে প্রতিরোধ করার জন্য আমি **Parameterized Queries (`$1, $2`)** ব্যবহার করেছি। এটি ডাটাবেসকে নির্দেশ দেয় যে, ক্লায়েন্টের পাঠানো ডাটাকে শুধুমাত্র "ভ্যালু" হিসেবে ট্রিট করতে হবে, কোনো "কমান্ড" হিসেবে নয়।

### ২.৩. Atomic Partial Updates (`COALESCE`)
```sql
UPDATE users SET name=COALESCE($1, name), age=COALESCE($3, age) WHERE id=$5 RETURNING *
```
MongoDB তে আমরা `$set` ব্যবহার করে আংশিক ডাটা (Partial Update) আপডেট করি। SQL এ এই কাজটি করার জন্য আমি `COALESCE` ফাংশন ব্যবহার করেছি। 
ফ্রন্টএন্ড থেকে যদি ইউজার শুধু `age` পাঠায় (নাম না পাঠায়), তবে `$1` এর মান হবে `NULL`। `COALESCE` ফাংশনটি প্রথম নন-নাল ভ্যালুটি নেয়। তাই নাম `NULL` হলে সে ডাটাবেসে থাকা আগের নামটাই রেখে দিবে। এটি ডাটাবেস লেভেলে একটি সম্পূর্ণ অ্যাটমিক (Atomic) অপারেশন, যা হাই-ট্রাফিক সিস্টেমে রেস কন্ডিশন (Race Condition) প্রতিরোধ করে।

---

## ৩. এক্সপ্রেস জেএস এবং নোড.জেএস এর কোর মেকানিজম

### ৩.১. Buffer Parsing (The Illusion of JSON)
```typescript
app.use(express.json());
```
ক্লায়েন্ট যখন সার্ভারে ডাটা পাঠায়, তখন সেটি JSON হিসেবে আসে না, আসে ০ এবং ১ এর বাইনারি বাফার (Buffer) স্ট্রিম হিসেবে। V8 ইঞ্জিন সরাসরি এই বাফার বুঝতে পারে না। `express.json()` মিডলওয়্যারটি সেই বাফারগুলোকে একসাথে করে (Chunking), তারপর মেমোরিতে স্ট্রিংয়ে কনভার্ট করে এবং সবশেষে `JSON.parse()` করে `req.body` তে যুক্ত করে। 

### ৩.২. The "Double Header" Bug Fix
কোড লেখার সময় আমি একটি ক্লাসিক বাগ ফেস করেছিলাম: `ERR_HTTP_HEADERS_SENT`। 
```typescript
if (result.rows.length === 0) {
  return res.status(404).json({ message: 'User Not found' });
}
```
প্রথমে আমার কোডে `return` ছিল না। ফলে ইউজার না পেলে সার্ভার `404` সেট করতো ঠিকই, কিন্তু ফাংশন এক্সিকিউশন থামতো না। নিচের লাইনে গিয়ে সে আবার `200 OK` পাঠানোর চেষ্টা করতো, যা Node.js এর আর্কিটেকচারে সম্পূর্ণ অবৈধ। এই `return` বসানোর মাধ্যমে আমি সার্ভার ক্র্যাশ হওয়া থেকে বাঁচিয়েছি।

---

## উপসংহার

MERN স্ট্যাকে কাজ করতে করতে অনেক সময় আমরা বেসিক জিনিসগুলো ভুলে যাই। এই প্রজেক্টটি করার পর রিলেশনাল ডাটাবেস, কানেকশন ম্যানেজমেন্ট এবং এক্সপ্রেসের লো-লেভেল অপারেশন নিয়ে আমার কনসেপ্ট আগের চেয়ে অনেক বেশি ক্লিয়ার হয়েছে।

**ভবিষ্যতের লক্ষ্য:**
পরবর্তী ধাপে আমি এই প্রোজেক্টটিতে `bcrypt` দিয়ে পাসওয়ার্ড হ্যাশিং, `Zod` দিয়ে ভ্যালিডেশন এবং পেজিনেশন (Pagination) যুক্ত করবো। একজন ফ্রেমওয়ার্ক-ডিপেন্ডেন্ট কোডার থেকে সত্যিকারের ইঞ্জিনিয়ার হওয়ার এই যাত্রা চলতে থাকবে!
