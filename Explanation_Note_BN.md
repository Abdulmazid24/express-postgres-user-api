# Express.js & PostgreSQL: My Personal Deep Dive Notes (Mission-2, Module-7)

**Author:** Abdul Mazid
**Email:** abdulmazid.dev@gmail.com
**Phone:** 01621455174
**Date:** May 15, 2026

প্রোগ্রামিং হিরোর নেক্সট লেভেল ওয়েব ডেভেলপমেন্ট কোর্সের মিশন-২, মডিউল-৭ কমপ্লিট করার পর আমার মনে হলো, শুধু ভিডিও দেখে কোড করলেই হবে না। একজন ভালো ইঞ্জিনিয়ার হতে হলে প্রজেক্টের প্রতিটি ফাইল এবং প্রতিটি লাইনের পেছনের কারণটা বুঝতে হবে। 

তাই আমি নিজে থেকে এক্সপ্রেস এবং নোড-পোস্টগ্রেসের অফিশিয়াল ডকুমেন্টেশন ঘেঁটে এই নোটটি তৈরি করেছি। এখানে আমি আমার কোডের বেসিক থেকে শুরু করে একদম হুডের নিচে (Under the hood) কীভাবে কাজ হচ্ছে, তা ধাপে ধাপে লিখে রাখলাম। এটি আমার নিজের লার্নিং ডায়েরি হিসেবে কাজ করবে।

---

## ১. প্রজেক্ট কনফিগারেশন

### ১.১. `package.json`
```json
"dependencies": { "express": "^5.2.1", "pg": "^8.20.0" },
"devDependencies": { "typescript": "^6.0.3" }
```
এখানে সবচেয়ে গুরুত্বপূর্ণ বিষয় হলো প্যাকেজের ভার্সনগুলো। আমি প্রজেক্টটি করার সময় একদম লেটেস্ট রিলিজগুলো ব্যবহার করেছি:
*   **Express 5.2.1:** এক্সপ্রেসের এই নতুন ভার্সনে একটি বড় সুবিধা হলো, এটি অ্যাসিনক্রোনাস কোডের এররগুলো নিজে থেকেই ক্যাচ করতে পারে। আগের ভার্সনগুলোতে রাউটের ভেতর `try/catch` না লিখলে সার্ভার ক্র্যাশ করার ভয় থাকতো, এখন সেটা নেই।
*   **pg 8.20.0:** এটি Node.js কে ডাটাবেসের সাথে কানেক্ট করে। এর নতুন আপডেটে `onConnect` নামে একটি ফিচার এসেছে যা ডাটাবেস কানেকশন আরও দ্রুত করে।
*   **TypeScript 6.0.3:** আমি `tsconfig.json` এ `es2025` সাপোর্ট অন করে রেখেছি। টাইপস্ক্রিপ্ট আমাকে ডেভেলপমেন্টের সময় ভুল ধরতে সাহায্য করে, কিন্তু মজার ব্যাপার হলো বিল্ড করার পর এই টাইপের কোনো অস্তিত্বই JS ফাইলে থাকে না।

### ১.২. `src/config/env.ts`
```typescript
import dotenv from 'dotenv';
import path from 'path';
dotenv.config({ path: path.join(process.cwd(), '.env') });
```
ডাটাবেসের পাসওয়ার্ড সরাসরি কোডে লেখাটা সিকিউরিটির জন্য রিস্কি। তাই আমি `.env` ফাইলে ডাটা রেখেছি এবং `dotenv` লাইব্রেরি দিয়ে সেগুলোকে `process.env` তে লোড করেছি। `process.cwd()` ব্যবহার করার কারণে আমি যে ফোল্ডার থেকেই সার্ভার রান করি না কেন, এটি ঠিকমতো `.env` ফাইলটি খুঁজে পাবে।

---

## ২. মেইন সার্ভার ফাইল (`server.ts`)

### ২.১. ইম্পোর্ট ও মিডলওয়্যার
```typescript
const app: Application = express();
app.use(express.json());
app.use(express.urlencoded({ extended: true }));
```
*   `express.json()`: ইন্টারনেট দিয়ে ডাটা আদান-প্রদান হয় বাফার (Buffer) হিসেবে। ফ্রন্টএন্ড থেকে যখন কোনো JSON পাঠানো হয়, এই মিডলওয়্যারটি সেই বাফারকে পার্স করে জাভাস্ক্রিপ্ট অবজেক্টে রূপান্তর করে এবং `req.body` তে যুক্ত করে।
*   `urlencoded({ extended: true })`: এটি ফর্ম ডাটা পার্স করার জন্য। `extended: true` দেওয়ার কারণে এটি `qs` লাইব্রেরি ব্যবহার করে নেস্টেড ডাটাও (যেমন Array বা Object) পার্স করতে পারে।

### ২.২. ডাটাবেস কানেকশন (Connection Pool)
```typescript
const pool = new Pool({ connectionString: config.connection_string });
```
ডাটাবেসের সাথে প্রতিবার নতুন করে কানেকশন তৈরি করা (TCP/TLS হ্যান্ডশেক) অনেক সময়সাপেক্ষ। তাই আমি `Pool` ব্যবহার করেছি। এটি সার্ভার চালু হওয়ার সাথে সাথেই ডাটাবেসের সাথে কয়েকটি কানেকশন তৈরি করে রেডি রাখে। যখনই কোনো রিকোয়েস্ট আসে, এটি রেডিমেড কানেকশন ব্যবহার করে, ফলে রেসপন্স টাইম অনেক কমে যায়।

### ২.৩. ডাটাবেস টেবিল স্কিমা
```typescript
CREATE TABLE IF NOT EXISTS users(
  id SERIAL PRIMARY KEY,
  name VARCHAR(20),
  email VARCHAR(20) UNIQUE NOT NULL,
  created_at TIMESTAMP DEFAULT NOW()
)
```
*   `SERIAL`: এটি ডাটাবেসে একটি সিকোয়েন্স তৈরি করে, ফলে নতুন ডাটা আসলে আইডি অটোমেটিক ১ করে বাড়তে থাকে।
*   `PRIMARY KEY`: এটি শুধু ইউনিক আইডেন্টিফায়ারই নয়, এটি ব্যাকএন্ডে B-Tree ইনডেক্সিং করে দেয়, যাতে ডাটা সার্চ করতে খুব কম সময় লাগে।
*   `UNIQUE NOT NULL`: ইমেইলের ক্ষেত্রে আমি এটি দিয়েছি যাতে কেউ ডুপ্লিকেট ইমেইল দিয়ে অ্যাকাউন্ট খুলতে না পারে। ডুপ্লিকেট ইমেইল আসলেই ডাটাবেস লেভেল থেকে সরাসরি এরর জেনারেট হবে।

---

## ৩. এপিআই রাউটিং (CRUD Operations)

### ৩.১. Create User (POST)
```typescript
app.post('/api/users', async (req: Request, res: Response) => {
  try {
    const { name, email, password, age } = req.body;
    const result = await pool.query(
      `INSERT INTO users(name,email,password,age) VALUES($1,$2,$3,$4) RETURNING *`,
      [name, email, password, age]
    );
  } catch (error: any) { res.status(500).json(...) }
});
```
*   **Parameterized Query (`$1, $2`):** আমি ইনপুটগুলোকে সরাসরি SQL এর ভেতর না বসিয়ে `$1` দিয়েছি। এটি SQL Injection হ্যাকিং থেকে আমার ডাটাবেসকে রক্ষা করবে।
*   **`RETURNING *`:** ইনসার্ট করার পর নতুন ডাটাটি দেখার জন্য আমাকে আবার `SELECT` চালাতে হয়নি। `RETURNING *` একই নেটওয়ার্ক কলের মধ্যে ডাটাটি ফেরত পাঠায়।

### ৩.২. Get Single User By ID (GET)
```typescript
app.get('/api/users/:id', async (req: Request, res: Response) => {
  const { id } = req.params;
```
ইউজার যদি `/api/users/5` হিট করে, তবে আমি `req.params.id` এর মাধ্যমে সেই `5` কে রিসিভ করে ডাটাবেসে সার্চ করছি। ডাটা না পেলে `404 Not Found` রিটার্ন করছি।

### ৩.৩. Update User (PUT)
```typescript
   UPDATE users SET name=COALESCE($1, name), password=COALESCE($2, password)... WHERE id=$5 RETURNING *
```
*   **`COALESCE` এর ব্যবহার:** এটি SQL এর একটি দারুণ ফাংশন। ফ্রন্টএন্ড থেকে যদি ইউজার শুধু নাম আপডেট করতে চায়, সে তো আর পাসওয়ার্ড পাঠাবে না। তখন পাসওয়ার্ড এর জায়গায় `NULL` আসবে। `COALESCE` ফাংশন চেক করে যে, নতুন ভ্যালু ফাঁকা হলে সে ডাটাবেসের আগের ভ্যালুটাই রেখে দিবে। এর ফলে একটি রাউট দিয়েই Partial Update বা PATCH এর কাজ হয়ে যাচ্ছে।

### ৩.৪. Delete User (DELETE)
```typescript
    const result = await pool.query(`DELETE FROM users WHERE id=$1`, [id]);
    if (result.rowCount === 0) { res.status(404).json(...); }
```
ডিলিট কুয়েরি কোনো ডাটা রিটার্ন করে না, সে শুধু জানায় কয়টা রো (row) ডিলিট হলো। তাই আমি `result.rowCount` চেক করেছি।

---

## আমার পরবর্তী টার্গেট

এই প্রজেক্টটি করার মাধ্যমে আমি ব্যাকএন্ডের কোর কনসেপ্টগুলো খুব ভালোভাবে বুঝতে পেরেছি। তবে একজন প্রফেশনাল ইঞ্জিনিয়ার হিসেবে সামনে আমাকে আরও কিছু জিনিস শিখতে হবে:
১. পাসওয়ার্ড এনক্রিপ্ট করার জন্য `bcrypt` ব্যবহার করা।
২. কোডগুলোকে একটি ফাইলের বদলে MVC (Model-View-Controller) প্যাটার্নে ভাগ করা।
৩. ডাটা ভ্যালিডেশনের জন্য `Zod` ব্যবহার করা।

আমার লার্নিং জার্নি এভাবেই চলতে থাকবে!
